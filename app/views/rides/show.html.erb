<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>zBikeGarage</title>
</head>

  <style>
    body {
      background-image: url(<%= asset_path "trail.jpg" %>);
    }
  </style>

  <body>
    <header>
      <%= render 'layouts/header' %>
    </header>

    <nav>
      <%= render 'layouts/navbar' %>
    </nav>

    <div class="container">
      <%= render @ride %> 
      <%= link_to "Edit Ride", edit_ride_path(@ride) %> <br>
      <%= button_to "Destroy Ride", @ride, method: :delete, style: "border: none; background-color: transparent; color: inherit; cursor: pointer; font-family: Arial; font-size: 17px; text-decoration: underline; text-decoration-color: blue;"%>
    </div>

    <div class="sidebar-container-right">
      <%= render 'layouts/sidebarright' %>
    </div>

    <div class="sidebar-container-left">
      <%= render 'layouts/sidebarleft' %>
    </div>

    <footer>
      <%= render 'layouts/footer' %>
    </footer>

  </body>
</html> 